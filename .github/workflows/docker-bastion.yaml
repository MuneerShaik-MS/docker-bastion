name: docker bastion
on:
  workflow_dispatch:
jobs:
  docker-bastion:
    runs-on: ubuntu-latest
    steps:
    - name: Copy Repo Files
      uses: actions/checkout@v5
    - name: check files
      run: ls -l
        